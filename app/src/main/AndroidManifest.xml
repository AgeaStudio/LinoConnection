<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.linonew">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".distributor.PreviewDeskripsiProduk"></activity>
        <activity android:name=".distributor.PreviewKatalog" />
        <activity android:name=".distributor.tambah_katalog" />
        <activity android:name=".distributor.Katalog" />
        <activity android:name=".distributor.editslot" />
        <activity android:name=".distributor.belislot" />
        <activity android:name=".distributor.slot" />
        <activity android:name=".distributor.MenuActivity" />
        <activity android:name=".TermCondition" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>